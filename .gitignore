__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.DS_Store

# LaTeX build artefacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz

# Results are committed deliberately: every figure ships with the CSV behind it, which is
# the point. Two exceptions, both regenerable from the logged seed:
results/scratch/
# 11 MB of every observable at every condition for every one of 5005 patients. No figure
# reads it; the labelled per-patient summary that figures do read is committed.
results/population_conditions.csv.gz
# The run log is a transcript, not a result.
results_run.log

# Playwright MCP scratch output from browser checks.
.playwright-mcp/
